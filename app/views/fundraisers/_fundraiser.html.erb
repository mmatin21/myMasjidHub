<div id="<%= fundraiser %>">
  <% progress = [fundraiser.donations.sum(:amount) / fundraiser.goal_amount * 100, 100].min %>
  <div class="rounded-lg border border-gray-200 bg-white p-6 shadow-sm dark:border-gray-700 dark:bg-gray-800">
    <div class="pt-6">
      <div class="flex items-center justify-between gap-1">
        <%= link_to fundraiser.name, fundraiser, class: "text-lg font-semibold leading-tight text-gray-900 hover:underline dark:text-white" %>
      </div>
      <p class="font-normal text-gray-700 dark:text-gray-400"><%= fundraiser.description %></p>
      <div class="mt-2 flex items-center gap-2">
        <div class="w-full bg-gray-200 rounded-full h-2.5 dark:bg-gray-700">
          <div class="bg-green-400 h-2.5 rounded-full" style="width: <%= progress %>%"></div>
        </div>
        <p class="text-sm font-medium text-gray-900 dark:text-white"><%= number_to_currency(fundraiser.goal_amount) %></p>
      </div>
      <div class="mt-4 flex items-center justify-between gap-4">
        <p class="tfont-normal text-gray-700 dark:text-gray-400">Raised Amount</p>
        <p class="text-1xl font-extrabold leading-tight text-gray-900 dark:text-white"><%= number_to_currency(fundraiser.donations.sum(:amount)) %></p>
      </div>
      <div class="mt-4 flex items-center justify-between gap-4">
        <p class="tfont-normal text-gray-700 dark:text-gray-400">Created At</p>
        <p class="text-1xl font-extrabold leading-tight text-gray-900 dark:text-white"><%= fundraiser.created_at.strftime("%b %d, %Y") %></p>
      </div>
    </div>
  </div>
</div>