<!DOCTYPE html>
<html>
  <head>
    <title>MosqueApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-blue-50 to-blue-00 dark:from-gray-900 dark:to-gray-800">
    <main>
      <%= turbo_frame_tag "turbo-modal" %>
      <% unless masjid_signed_in? and current_masjid.address.nil? %>
        <%= render "sidebar/index" %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
