<div class="antialiased">
  <section class="p-4 md:ml-64 h-auto pt-20">
    <% content_for :title, "Expenses" %>
    <div class="flex justify-between items-center">
      <h1 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 dark:text-white">Expenses</h1>
    </div>
    <div class="border-gray-300 dark:border-gray-600 rounded-xl h-128">
      <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
        <%= render "tables/table", table: @table_expenses, query: @q, pagy: @pagy, model: :expense %>
      </div>
    </div>
  </section>
</div>
